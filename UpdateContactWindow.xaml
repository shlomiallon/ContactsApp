<Window x:Class="ContactsApp.UpdateContactWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Update Contact" Height="300" Width="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Label Content="First Name:" Grid.Row="0" Margin="10"/>
        <TextBox x:Name="firstNameTextBox" Grid.Row="0" Margin="10" Width="200" Text="{Binding firstName, Mode=TwoWay}"/>
        <Label Content="Last Name:" Grid.Row="1" Margin="10"/>
        <TextBox x:Name="lastNameTextBox" Grid.Row="1" Margin="10" Width="200" Text="{Binding lastName, Mode=TwoWay}"/>
        <Label Content="Phone Number:" Grid.Row="2" Margin="10"/>
        <TextBox x:Name="phoneNumberTextBox" Grid.Row="2" Margin="10" Width="200" Text="{Binding phoneNumber, Mode=TwoWay}"/>
        <Button Content="Save" Grid.Row="3" Margin="10" Click="SaveButton_Click" HorizontalAlignment="Center"/>
    </Grid>
</Window>

